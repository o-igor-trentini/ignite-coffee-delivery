import { FC } from 'react';
import '../styles/main.css';
import { Header } from './layout/Header';

export const App: FC = () => {
    return (
        <div>
            <Header />
        </div>
    );
};
